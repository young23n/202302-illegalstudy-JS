1. 아래의 코드에서 표현식인 부분과 표현식이 아닌 부분에 대해서 구분하시오

    ----------------------------표현식O
    x=100;
    console.log(a);
    var a = y = 100;
    ----------------------------표현식X
    var x;
    var foo = var x;

2.그렇다면 위에 설명과 같이 다 실수로 측정한다면 2진수, 8진수, 16진수를 출력
하면 어떤식으로 될까?
    모두 65로 통일된 값이 출력된다.
